export interface Project {
  title: string
  description: string
  tech: string[]
  github?: string
  demo?: string
  year?: string
}

export const projects: Project[] = [
  // 在此添加项目数据。格式见上方 Project 接口定义。
]
